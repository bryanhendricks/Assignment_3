import java.util.ArrayList;
public class WorldModel extends Background{
    public WorldModel(double num_rows, double num_cols, Background background){
        super(num_rows, num_cols, background);
    }
}